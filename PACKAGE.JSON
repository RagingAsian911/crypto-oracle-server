"name": "@ragingasian911/bbw-temple-site",
"version": "1.0.1", // bump this for every publish
"main": "MAIN.JS",
"files": ["MAIN.JS", "server/", "index.html"],
"publishConfig": {
  "registry": "https://npm.pkg.github.com/"
}

  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "axios": "^1.6.0"
  }
}
"publishConfig": {
  "registry": "https://npm.pkg.github.com/"
}
"name": "@ragingasian911/bbw-temple-site",
"version": "1.0.0",
"main": "MAIN.JS",
"files": ["MAIN.JS", "server/", "index.html"],
"publishConfig": {
  "registry": "https://npm.pkg.github.com/"
}
env:
  NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}{
  "name": "crypto-oracle-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:dev": "nodemon server.js",
    "test": "node ./tests/run-tests.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "helmet": "^6.0.1",
    "morgan": "^1.10.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
