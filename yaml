on:
  push:
    branches:
      - main
registry-url: 'https://npm.pkg.github.com/'
jobs:
  publish:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./crypto-oracle-server
